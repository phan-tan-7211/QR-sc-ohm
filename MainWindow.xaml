<Window x:Class="UC2836Live.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="UC2836CX+ · Live View" Width="1560" Height="860" MinWidth="1100" MinHeight="720" Background="#EEF3F7" Foreground="#142D43" FontFamily="Segoe UI" FontSize="14" Closing="Window_Closing">
 <Grid Margin="24">
  <Grid.ColumnDefinitions>
   <ColumnDefinition Width="*"/>
   <ColumnDefinition Width="20"/>
   <ColumnDefinition Width="680"/>
  </Grid.ColumnDefinitions>

  <!-- ========== CỘT TRÁI: nội dung đo ========== -->
  <Grid Grid.Column="0">
   <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <Grid><StackPanel><TextBlock Text="UC2836CX+" FontSize="30" FontWeight="Bold"/><TextBlock Text="TRẠM ĐO CUỘN DÂY  /  DEV BY PHANTAN7211@GMAIL.COM" Foreground="#435E73" Margin="0,4,0,18"/></StackPanel><StackPanel HorizontalAlignment="Right"><TextBlock x:Name="Status" Text="● Chưa kết nối" Foreground="#087567" HorizontalAlignment="Right"/><TextBlock x:Name="Identity" Text="USB CDC · SCPI" Foreground="#435E73" Margin="0,8,0,0"/></StackPanel></Grid>
   <Border Grid.Row="1" Background="{StaticResource Panel}" CornerRadius="10" Padding="16" Margin="0,0,0,16"><WrapPanel VerticalAlignment="Center">
    <TextBlock Text="Cổng" VerticalAlignment="Center" Margin="0,0,8,0"/><ComboBox x:Name="Port" IsEditable="True" Text="COM3"/>
    <TextBlock Text="Baud" VerticalAlignment="Center" Margin="0,0,8,0"/><ComboBox x:Name="Baud" SelectedIndex="0"><ComboBoxItem Content="9600"/><ComboBoxItem Content="19200"/><ComboBoxItem Content="38400"/><ComboBoxItem Content="115200"/></ComboBox>
    <Button x:Name="Refresh" Content="Tìm cổng" Click="Refresh_Click"/>
    <Button x:Name="Start" Content="Kết nối" Background="#087E75" Foreground="White" Click="ToggleConnection_Click"/>
    <Button Content="Xuất CSV…" Click="Export_Click"/>
    <TextBlock Text="Chu kỳ đọc (ms)" VerticalAlignment="Center" Margin="8,0,8,0"/><ComboBox x:Name="Interval" SelectedIndex="1" MinWidth="75"><ComboBoxItem Content="50"/><ComboBoxItem Content="100"/><ComboBoxItem Content="250"/><ComboBoxItem Content="500"/></ComboBox>
   </WrapPanel></Border>
   <UniformGrid Grid.Row="2" Columns="3" Margin="0,0,0,14">
    <Border Background="{StaticResource Panel}" CornerRadius="10" Padding="18" Margin="0,0,10,0"><StackPanel><TextBlock x:Name="PrimaryLabel" Text="LA · CUỘN A" Foreground="#435E73"/><TextBlock x:Name="LA" Text="—" FontSize="28" FontWeight="SemiBold" Foreground="#087567" Margin="0,8"/><TextBlock Text="Đơn vị tự động" Foreground="#435E73" FontSize="12"/></StackPanel></Border>
    <Border Background="{StaticResource Panel}" CornerRadius="10" Padding="18" Margin="0,0,10,0"><StackPanel><TextBlock x:Name="SecondaryLabel" Text="LB · CUỘN B" Foreground="#435E73"/><TextBlock x:Name="LB" Text="—" FontSize="28" FontWeight="SemiBold" Foreground="#175FAC" Margin="0,8"/><TextBlock Text="Đơn vị tự động" Foreground="#435E73" FontSize="12"/></StackPanel></Border>
    <Border Background="{StaticResource Panel}" CornerRadius="10" Padding="18"><StackPanel><TextBlock x:Name="CLabel" Text="TRẠNG THÁI MẪU" Foreground="#435E73"/><TextBlock x:Name="CValue" Text="—" FontSize="28" FontWeight="SemiBold" Margin="0,8"/><TextBlock x:Name="DcText" Text="Kết quả phân loại từ thiết bị" Foreground="#435E73" FontSize="12"/></StackPanel></Border>
   </UniformGrid>
   <!-- QR vẫn nhận được từ camera; các trường ẩn giữ tương thích với máy quét USB và logic cũ. -->
   <StackPanel Visibility="Collapsed"><TextBox x:Name="QrInput" KeyDown="QrInput_KeyDown"/><TextBlock x:Name="QrState" Text="Chưa quét mã"/></StackPanel>
   <!-- Hai trạng thái vẫn được giữ làm dữ liệu nội bộ để CSV/logic phân loại hoạt động,
        nhưng không chiếm diện tích giao diện. Kết quả xem trong bảng lịch sử. -->
   <StackPanel Visibility="Collapsed"><TextBlock x:Name="Compare"/><TextBlock x:Name="Wire"/></StackPanel>
   <StackPanel Grid.Row="3" Margin="0,0,0,12"><TextBlock x:Name="Settings" Text="Đọc cấu hình khi bắt đầu. Ứng dụng không thay đổi thông số đo."/><TextBlock x:Name="Second" Text="" Foreground="#435E73" Margin="0,5,0,0"/></StackPanel>
   <Border Grid.Row="4" Background="{StaticResource Panel}" CornerRadius="10" Padding="14" Margin="0,0,0,14"><Grid>
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <DockPanel><TextBlock Text="XU HƯỚNG  ·  150 MẪU GẦN NHẤT" FontWeight="SemiBold"/><TextBlock x:Name="ChartLegend" Text="LA  ━ xanh ngọc     LB  ━ xanh lam" HorizontalAlignment="Right" Foreground="#435E73"/></DockPanel>
    <Canvas x:Name="Chart" Grid.Row="1" ClipToBounds="True" SizeChanged="Chart_SizeChanged" Margin="0,8" Background="#F5F8FB"><Polyline x:Name="LineA" Stroke="#087567" StrokeThickness="2"/><Polyline x:Name="LineB" Stroke="#175FAC" StrokeThickness="2"/></Canvas>
    <TextBlock x:Name="Scale" Grid.Row="2" Text="Trục ngang: thứ tự mẫu nhận · Trục dọc: giá trị LA/LB nguyên bản" FontSize="12" Foreground="#435E73"/>
   </Grid></Border>
   <StackPanel Grid.Row="5" Margin="0,12,0,0"><TextBlock x:Name="Stats" Text="0 mẫu · Bảng giữ 2.000 mẫu gần nhất; CSV tự lưu toàn bộ phiên." Foreground="#435E73"/><TextBlock x:Name="Raw" Visibility="Collapsed" Text="Dữ liệu: —" FontFamily="Consolas" FontSize="12" TextTrimming="CharacterEllipsis" Margin="0,5,0,0"/><TextBlock x:Name="LogPath" Foreground="#435E73" FontSize="12" Margin="0,5,0,0" TextTrimming="CharacterEllipsis"/></StackPanel>
  </Grid>

  <!-- ========== CỘT PHẢI: camera QR + lịch sử đo ========== -->
  <Grid Grid.Column="2">
   <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="12"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Border Background="{StaticResource Panel}" CornerRadius="10" Padding="16">
   <Grid>
    <Grid.RowDefinitions>
     <RowDefinition Height="Auto"/>
     <RowDefinition Height="Auto"/>
     <RowDefinition Height="Auto"/>
     <RowDefinition Height="Auto"/>
     <RowDefinition Height="Auto"/>
     <RowDefinition Height="Auto"/>
     <RowDefinition Height="*"/>
     <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Tiêu đề -->
    <DockPanel Margin="0,0,0,12">
     <Button DockPanel.Dock="Right" Content="⚙" Click="CamDevSettings_Click" Padding="6,3" FontSize="13" MinHeight="0" ToolTip="Cài đặt thông số dev" Background="#DEEAF1" VerticalAlignment="Top"/>
     <StackPanel>
      <TextBlock Text="CAMERA QR" FontWeight="Bold" FontSize="15"/>
      <TextBlock Text="Quét tự động" Foreground="#435E73" FontSize="12" Margin="0,3,0,0"/>
     </StackPanel>
    </DockPanel>

    <!-- Nguồn + nút bật/tắt -->
    <DockPanel Grid.Row="1" Margin="0,0,0,8" LastChildFill="True">
     <Button x:Name="CamToggle" DockPanel.Dock="Right" Content="Bật" Click="CamToggle_Click" Background="#087E75" Foreground="White" Padding="10,8" MinHeight="36" Margin="6,0,0,4"/>
     <ComboBox x:Name="CamSources" MinWidth="60" Padding="6,6" MinHeight="36" Margin="0,0,0,4"/>
    </DockPanel>

    <!-- Zoom -->
    <Grid Grid.Row="2" Margin="0,0,0,6">
     <Grid.ColumnDefinitions><ColumnDefinition Width="42"/><ColumnDefinition Width="*"/><ColumnDefinition Width="38"/></Grid.ColumnDefinitions>
     <TextBlock Text="Zoom" VerticalAlignment="Center" FontSize="12"/>
     <Slider x:Name="CamZoom" Grid.Column="1" Minimum="1" Maximum="3" Value="1" SmallChange="0.1" VerticalAlignment="Center" ValueChanged="CamSettings_Changed"/>
     <TextBlock x:Name="CamZoomLabel" Grid.Column="2" Text="1.0×" VerticalAlignment="Center" FontSize="12" HorizontalAlignment="Right"/>
    </Grid>

    <!-- Checkboxes -->
    <WrapPanel Grid.Row="3" Margin="0,0,0,10">
     <CheckBox x:Name="CamEnhance" Content="Tương phản" IsChecked="False" FontSize="12" Margin="0,0,10,4" Checked="CamSettings_Changed" Unchecked="CamSettings_Changed"/>
     <CheckBox x:Name="CamInvert" Content="Đảo màu" IsChecked="False" FontSize="12" Margin="0,0,10,4" Checked="CamSettings_Changed" Unchecked="CamSettings_Changed"/>
     <CheckBox x:Name="CamSharpen" Content="Làm nét" IsChecked="False" FontSize="12" Margin="0,0,0,4" Checked="CamSettings_Changed" Unchecked="CamSettings_Changed"/>
     <CheckBox x:Name="CamShowProcessed" Content="Xem xử lý" IsChecked="False" FontSize="12" Margin="0,0,0,4" Checked="CamSettings_Changed" Unchecked="CamSettings_Changed"/>
    </WrapPanel>

    <!-- Preview camera -->
    <Border Grid.Row="4" Background="#102030" CornerRadius="8" Height="158" Margin="0,0,0,10">
     <Grid>
      <TextBlock x:Name="CamPlaceholder" Text="Camera chưa bật" Foreground="#AABBCC" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="12"/>
      <Image x:Name="CamPreview" Stretch="Uniform"/>
     </Grid>
    </Border>

    <!-- Mã QR vừa đọc -->
    <StackPanel Grid.Row="5" Margin="0,0,0,8">
     <DockPanel Margin="0,0,0,6">
      <TextBlock Text="MÃ QR VỪA QUÉT" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center"/>
      <Button DockPanel.Dock="Right" Content="Xóa / Quét lại" Click="CamReset_Click" Padding="8,5" MinHeight="0" FontSize="12" Margin="0,0,0,0"/>
     </DockPanel>
     <Border Background="#F0FAF8" CornerRadius="6" Padding="10,8">
      <TextBlock x:Name="CamQrResult" Text="—" FontSize="16" FontWeight="SemiBold" Foreground="#075F59" TextWrapping="Wrap"/>
     </Border>
    </StackPanel>

    <!-- Spacer -->
    <Grid Grid.Row="6"/>

    <!-- Trạng thái camera -->
    <TextBlock x:Name="CamStatus" Grid.Row="7" Text="Camera chưa bật" Foreground="#435E73" FontSize="12" TextWrapping="Wrap"/>
   </Grid>
  </Border>

  <!-- Lịch sử đo -->
  <Border Grid.Row="2" Background="{StaticResource Panel}" CornerRadius="10" Padding="8">
   <DataGrid x:Name="History" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" Background="#FFFFFF" Foreground="#172538" BorderThickness="0" RowHeaderWidth="0" AlternatingRowBackground="#EDF4FA" RowHeight="34" GridLinesVisibility="None" EnableRowVirtualization="True" EnableColumnVirtualization="True">
    <DataGrid.Resources><Style TargetType="TextBlock"><Setter Property="Foreground" Value="#172538"/></Style></DataGrid.Resources>
    <DataGrid.Columns>
     <DataGridTextColumn Header="Thời gian" Binding="{Binding TimeText}" Width="100"/>
     <DataGridTextColumn Header="Mã QR" Binding="{Binding QrCode}" Width="*"/>
     <DataGridTextColumn Header="Chính / LA" Binding="{Binding DisplayA}" Width="110"/>
     <DataGridTextColumn Header="Phụ / LB" Binding="{Binding DisplayB}" Width="110"/>
     <DataGridTextColumn Header="Kết quả" Binding="{Binding PassFail}" Width="100"/>
    </DataGrid.Columns>
   </DataGrid>
  </Border>
  </Grid>

 </Grid>
</Window>
